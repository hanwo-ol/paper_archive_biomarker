---
layout: page
title: 정밀도·재현성 설계
permalink: /precision-design/
---

# LOD/정밀도 통계 프레임

JBNU 전북대학교병원 × KIST 생체분자인식연구센터 공동연구과제 · 2026-09-21

## 설계 원칙: 세 가지 질문을 분리한다

프로토콜의 "T0/T1/T2 paired sampling"은 하나의 통계 문제가 아니라 서로 다른 세 가지 질문을 담고 있다. T0(응급실 도착)·T1(T0+3~6시간)·T2(입원 24±4시간)은 코펩틴 농도가 시간에 따라 자연 감소하는 것이 정상인 임상 동역학 시점이다. 이 세 시점의 값을 그대로 신뢰도 지표(ICC)에 넣으면 측정오차와 실제 생물학적 변화가 섞여 신뢰도가 인위적으로 낮게 나온다. 세 질문을 아래처럼 분리해야 한다.

| 질문 | 필요한 데이터 | 통계 방법 |
|---|---|---|
| 센서가 같은 검체를 반복 측정해도 같은 값을 내는가(기술적 재현성) | 동일 혈액 검체의 카트리지 중복 측정 | ICC(신뢰구간 기반 표본크기) |
| 센서 값이 상용 면역분석(코펩틴/S100β) 값과 일치하는가(방법 간 일치도) | 각 시점에서 센서-상용법 동시 측정 쌍 | Bland-Altman, subject 랜덤효과 mixed model |
| 시간에 따라 값이 예상대로 변하는가(임상 동역학) | T0/T1/T2 전체 궤적 | 선형혼합모형(시간 고정효과 + subject 랜덤절편) — 표본크기는 [표본크기 산정]({{ '/sample-size/' | relative_url }}) 페이지의 stroke 확진군 n(59명)을 그대로 사용 |

## 1. 센서 기술적 재현성 (ICC)

**계산식** (Bonett 2002, *Statistics in Medicine* — [10.1002/sim.1108](https://doi.org/10.1002/sim.1108); 분산 근사는 Fleiss 1986/Shrout & Fleiss 1979의 표준 ICC 분산식과 동일 구조):

```
n = 1 + 2·z_(α/2)²·(1-ρ)²·(1+(k-1)ρ)² / (k·(k-1)·w²)
```

ρ=계획 ICC, k=중복측정 횟수, w=목표 95% CI 반폭, z_(α/2)=1.96(α=0.05, 양측)

**신뢰도 해석 기준**: Koo & Li 2016, *J Chiropr Med* — [10.1016/j.jcm.2016.02.012](https://doi.org/10.1016/j.jcm.2016.02.012). ICC<0.5 poor, 0.5-0.75 moderate, 0.75-0.9 good, >0.9 excellent.

| ρ(계획 ICC) | k(중복측정) | w(목표 CI 반폭) | 필요 표본수 |
|---|---|---|---|
| 0.90 | 2 | ±0.10 | **15명** |
| 0.90 | 2 | ±0.05 | 57명 |
| 0.90 | 3 | ±0.10 | 12명 |
| 0.85 | 2 | ±0.10 | 31명 |

> "혈량 50 µL 이하" 제약이 중복측정 횟수 k를 제한한다. k=2(중복)는 100 µL로 제약 내에서 가능하지만, k=3(삼중)은 150 µL로 단일 채혈 배정량을 초과할 위험이 있다. ρ=0.90(excellent), k=2, w=±0.10 조합을 권장 설계로 제시하며, 이 경우 검체 15건으로 검정력 0.80을 달성한다.

## 2. 센서-상용법 일치도 (Bland-Altman)

**방법**: Lu et al. 2016, *Int J Biostat* — [10.1515/ijb-2015-0039](https://doi.org/10.1515/ijb-2015-0039). 이 방법은 일치한계(LOA)의 신뢰구간 폭을 사전에 정한 임상적 허용 한계(Δ)와 비교해 표본수를 정한다. Sedgwick 2013, *BMJ* — [10.1136/bmj.f1630](https://doi.org/10.1136/bmj.f1630)은 이 방법의 해석을 임상 연구자 대상으로 설명한다.

> 표본수를 확정하려면 Δ(센서와 상용법의 차이가 임상적으로 허용 가능한 최대 폭, 예: 코펩틴 ±X pmol/L)가 필요하다. 이 값은 프로토콜 문서에 없으며 검사의학과/신경과 임상팀이 정해야 하는 값이다. Δ를 받으면 위 공식에 대입해 표본수를 바로 산출할 수 있다.

측정 쌍은 T0/T1/T2 세 시점에서 반복적으로 나오므로(같은 환자가 3쌍을 제공), subject를 랜덤효과로 둔 mixed model로 시점 간 상관을 반영해야 한다 — 세 시점을 독립 표본처럼 다루면 유효 표본수를 과대평가한다.

## 3. 임상 동역학 (참고)

T0→T1→T2 궤적이 코펩틴의 알려진 반감기(수 시간)에 부합하는 하강 패턴을 보이는지는 표본크기 문제가 아니라 모형 설계 문제다. 시간을 고정효과, subject를 랜덤절편으로 둔 선형혼합모형(`value ~ time + (1|subject)`)을 적용한다 — CLAUDE.md의 반복측정 subject 그룹화 원칙과 동일하다. 이 분석의 표본수는 별도로 계산하지 않고 [표본크기 산정]({{ '/sample-size/' | relative_url }}) 페이지에서 산출한 stroke 확진군 59명을 그대로 쓴다.

---

재현 스크립트: `sample_size_calc.py` (섹션 4, CHEN venv)

[← 홈으로]({{ '/' | relative_url }})
